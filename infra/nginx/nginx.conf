server {
    listen 80;
    server_name routepick.net www.routepick.net;

    # =========================
    # 🌐 1) React 정적 파일 제공
    # =========================
    root /usr/share/nginx/html;
    index index.html;

    # SPA(React Router) 지원
    location / {
        try_files $uri /index.html;
    }

    # ===================================
    # 🚀 2) API Reverse Proxy (Spring Boot)
    # ===================================
    location /api/ {
        proxy_pass http://backend:4000/api/;   # backend = 도커 서비스 이름
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ======================================
    # 📂 3) 파일 경로 Reverse Proxy
    #     (ex. https://routepick.net/file/...)
    # ======================================
    location /file/ {
        proxy_pass http://backend:4000/file/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
