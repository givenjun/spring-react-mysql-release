name: 🚀 Auto Deploy to EC2 (Backend + Frontend Build in Actions)

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # 1. 체크아웃
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2. 환경 설정 (버전 고정 필수)
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    # ===============================
    # Backend Build
    # ===============================
    - name: Build Backend
      run: |
        cd board-back
        chmod +x ./gradlew
        ./gradlew clean bootJar -x test

    # [중요] JAR 파일 이름 변경 단계 추가
    - name: Rename Backend Jar
      run: |
        cd board-back/build/libs
        mv *.jar app.jar

    # ===============================
    # Frontend Build
    # ===============================
    - name: Build Frontend
      run: |
        cd board-front
        npm ci
        npm run build

    # ===============================
    # Clean Remote Directory (청소)
    # ===============================
    # 기존 프론트엔드 파일을 지우지 않으면 파일이 계속 쌓이고 캐싱 문제가 생길 수 있음
    - name: Clean Remote Frontend Directory
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_KEY }}
        script: rm -rf /home/ubuntu/routepick/frontend/*

    # ===============================
    # Upload Backend JAR
    # ===============================
    - name: Upload Backend Jar
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_KEY }}
        source: "board-back/build/libs/app.jar"
        target: "/home/ubuntu/routepick/backend/app.jar" 

    # ===============================
    # Upload Frontend Build
    # ===============================
    - name: Upload Frontend Build
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_KEY }}
        source: "board-front/build/*" # build 폴더 안의 내용물만
        target: "/home/ubuntu/routepick/frontend/"
        strip_components: 2 # [핵심] board-front/build/ 경로 2단계를 제거

    # ===============================
    # Restart Docker
    # ===============================
    - name: Restart EC2 Docker Services
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_KEY }}
        script: |
          echo "📦 RoutePick 배포 시작"
          cd /home/ubuntu/routepick/infra

          echo "📌 기존 컨테이너 종료"
          docker compose -f docker-compose.prod.yml down

          echo "📌 컨테이너 재시작"
          docker compose -f docker-compose.prod.yml up -d
          
          echo "🧹 미사용 이미지 정리"
          docker image prune -f

          echo "🎉 배포 완료!"